# Editor behaviour

set autoedit=yes
set ispell="aspell -e -c"
set sig_dashes

## Make plain text mail linebreaks work for readers
set text_flowed=yes

## Make Neovim work with flowed text and add spell checking
set editor='nvim "+setlocal spell spelllang=en_us,en_gb fo+=aw"'
set visual='nvim "+setlocal spell spelllang=en_us,en_gb fo+=aw"'

bind editor <Tab> complete-query
bind editor <space> noop
bind editor ^T complete
